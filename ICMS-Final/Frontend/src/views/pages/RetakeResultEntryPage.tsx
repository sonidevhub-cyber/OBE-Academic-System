import React from 'react';

import RetakeAssessmentWrapper from '../../features/retake/RetakeAssessmentWrapper';

const RetakeResultEntryPage: React.FC = () => {
  return <RetakeAssessmentWrapper />;
};

export default RetakeResultEntryPage;
